# 🤖 بوت تلجرام للتحقق من توفر اليوزرات على إنستجرام

بوت متكامل يقوم بإنشاء يوزرات رباعية عشوائية للإنستجرام والتحقق من توفرها، مع حفظ النتائج في قاعدة بيانات.

## ✨ الميزات

- ✅ **إنشاء يوزرات عشوائية**: إنشاء يوزرات مكونة من 4 أحرف عشوائياً
- ✅ **التحقق من التوفر**: فحص توفر اليوزرات على إنستجرام بشكل فعال
- ✅ **قاعدة بيانات**: حفظ اليوزرات المتاحة في SQLite
- ✅ **واجهة تفاعلية**: استخدام أزرار في تلجرام للتفاعل السهل
- ✅ **الإحصائيات**: عرض إحصائيات شاملة عن الفحوصات
- ✅ **سجل الفحوصات**: تتبع كامل لجميع الفحوصات السابقة

## 📁 هيكل المشروع

```
instagram_bot/
├── username_generator.py      # نظام إنشاء اليوزرات العشوائية
├── instagram_checker.py       # نظام التحقق من توفر اليوزرات
├── database.py               # إدارة قاعدة البيانات
├── telegram_bot.py           # بوت تلجرام الرئيسي
├── test_bot.py              # ملف الاختبارات
├── requirements.txt         # المكتبات المطلوبة
└── README.md               # هذا الملف
```

## 🚀 البدء السريع

### 1. المتطلبات

- Python 3.9+
- pip (مدير الحزم)

### 2. التثبيت

```bash
# نسخ المشروع
git clone <repository-url>
cd instagram_bot

# تثبيت المكتبات
pip install -r requirements.txt
```

### 3. الإعداد

قبل تشغيل البوت، تحتاج إلى:

1. **الحصول على توكن تلجرام**:
   - تحدث مع [@BotFather](https://t.me/botfather) على تلجرام
   - أنشئ بوت جديد واحصل على التوكن

2. **تحديث التوكن في البوت**:
   - افتح ملف `telegram_bot.py`
   - ابحث عن السطر: `TOKEN = "YOUR_TELEGRAM_BOT_TOKEN"`
   - استبدل `YOUR_TELEGRAM_BOT_TOKEN` بالتوكن الخاص بك

### 4. التشغيل

```bash
# تشغيل البوت
python3 telegram_bot.py
```

## 📖 دليل الاستخدام

### أوامر البوت

| الأمر | الوصف |
|------|-------|
| `/start` | بدء البوت وعرض القائمة الرئيسية |
| `/help` | عرض المساعدة والأوامر المتاحة |
| `/stats` | عرض الإحصائيات |
| `/available` | عرض آخر اليوزرات المتاحة |

### الأزرار الرئيسية

- **🔄 إنشاء والتحقق**: إنشاء يوزرات جديدة والتحقق من توفرها
- **📊 عرض الإحصائيات**: عرض إحصائيات شاملة
- **📋 اليوزرات المتاحة**: عرض آخر اليوزرات المتاحة
- **ℹ️ معلومات**: معلومات عن البوت

## 🔧 شرح المكونات

### 1. username_generator.py

يقوم بإنشاء يوزرات عشوائية:

```python
from username_generator import UsernameGenerator

generator = UsernameGenerator()

# إنشاء يوزر واحد
username = generator.generate_single_username()

# إنشاء 10 يوزرات
usernames = generator.generate_multiple_usernames(10)
```

**الميزات**:
- إنشاء يوزرات مكونة من 4 أحرف
- تجنب التكرار تلقائياً
- دعم الأحرف والأرقام والشرطة السفلية

### 2. instagram_checker.py

يتحقق من توفر اليوزرات على إنستجرام:

```python
import asyncio
from instagram_checker import InstagramChecker

async def check():
    checker = InstagramChecker()
    
    # فحص يوزر واحد
    result = await checker.check_username_availability('username')
    
    # فحص عدة يوزرات
    results = await checker.check_multiple_usernames(['user1', 'user2'])

asyncio.run(check())
```

**الميزات**:
- فحص غير متزامن (Async) لتحسين الأداء
- معالجة الأخطاء والمهل الزمنية
- دعم الفحص على دفعات

### 3. database.py

إدارة قاعدة البيانات:

```python
from database import DatabaseManager

db = DatabaseManager()

# إضافة يوزرات
db.add_available_username('abcd')
db.add_multiple_usernames(['xyz1', 'user', 'test'])

# الحصول على البيانات
usernames = db.get_all_available_usernames()
count = db.get_available_usernames_count()

# الإحصائيات
stats = db.get_statistics()
```

**الميزات**:
- حفظ اليوزرات المتاحة
- سجل الفحوصات الكامل
- إحصائيات شاملة
- البحث والحذف

### 4. telegram_bot.py

بوت تلجرام الرئيسي:

```bash
python3 telegram_bot.py
```

**الميزات**:
- واجهة تفاعلية بالأزرار
- معالجة الأوامر
- رسائل تفاعلية
- معالجة الأخطاء

## 🧪 الاختبار

لتشغيل الاختبارات:

```bash
python3 test_bot.py
```

سيتم اختبار:
- ✅ مولد اليوزرات
- ✅ مدقق إنستجرام
- ✅ قاعدة البيانات

## 📊 قاعدة البيانات

البوت يستخدم SQLite مع الجداول التالية:

### جدول available_usernames
| العمود | النوع | الوصف |
|-------|------|-------|
| id | INTEGER | معرف فريد |
| username | TEXT | اليوزر |
| status | TEXT | الحالة |
| created_at | TIMESTAMP | وقت الإنشاء |
| checked_at | TIMESTAMP | وقت آخر فحص |
| notes | TEXT | ملاحظات |

### جدول check_history
| العمود | النوع | الوصف |
|-------|------|-------|
| id | INTEGER | معرف فريد |
| username | TEXT | اليوزر |
| available | BOOLEAN | هل متاح |
| status_code | INTEGER | رمز HTTP |
| error_message | TEXT | رسالة الخطأ |
| checked_at | TIMESTAMP | وقت الفحص |

## ⚙️ الإعدادات المتقدمة

### تغيير عدد الطلبات المتزامنة

في ملف `instagram_checker.py`:

```python
# تغيير حجم الدفعة (افتراضي: 5)
results = await checker.check_batch_usernames(usernames, batch_size=10)
```

### تغيير مسار قاعدة البيانات

في ملف `telegram_bot.py`:

```python
# استخدام مسار مختلف
db = DatabaseManager("/path/to/database.db")
```

## 🔒 الأمان

- البوت لا يخزن بيانات المستخدمين الشخصية
- التوكن يجب أن يكون سري (لا تشاركه مع أحد)
- الفحوصات تتم بشكل آمن عبر HTTPS

## 📝 الملاحظات المهمة

1. **حدود إنستجرام**: قد تواجه حدود في عدد الطلبات من إنستجرام
2. **التأخير**: هناك تأخير بين الطلبات لتجنب الحظر
3. **الدقة**: قد لا تكون النتائج 100% دقيقة دائماً
4. **الاستخدام المسؤول**: استخدم البوت بشكل مسؤول ولا تقم بإساءة الاستخدام

## 🐛 استكشاف الأخطاء

### البوت لا يستجيب
- تأكد من صحة التوكن
- تحقق من الاتصال بالإنترنت
- أعد تشغيل البوت

### خطأ في قاعدة البيانات
```bash
# حذف قاعدة البيانات وإعادة إنشاؤها
rm instagram_usernames.db
python3 telegram_bot.py
```

### مشاكل في الفحص
- تأكد من الاتصال بالإنترنت
- قد تكون مقيد من قبل إنستجرام، انتظر قليلاً

## 📚 المراجع

- [python-telegram-bot](https://python-telegram-bot.readthedocs.io/)
- [aiohttp Documentation](https://docs.aiohttp.org/)
- [SQLite Documentation](https://www.sqlite.org/docs.html)

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الشخصي والتعليمي.

## 💬 الدعم

إذا واجهت أي مشاكل أو لديك اقتراحات، يرجى فتح issue أو التواصل معنا.

---

**تم إنشاؤه بـ ❤️ لتسهيل البحث عن اليوزرات المتاحة على إنستجرام**
