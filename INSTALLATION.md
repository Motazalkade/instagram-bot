# 📦 دليل التثبيت والإعداد

دليل شامل لتثبيت وتشغيل بوت تلجرام للتحقق من اليوزرات.

## 📋 المتطلبات

- **نظام التشغيل**: Windows, macOS, أو Linux
- **Python**: الإصدار 3.9 أو أحدث
- **pip**: مدير الحزم (يأتي مع Python)
- **توكن تلجرام**: للحصول عليه، تحدث مع [@BotFather](https://t.me/botfather)

## 🔧 خطوات التثبيت

### الخطوة 1: تحميل المشروع

#### الطريقة الأولى: استخدام Git
```bash
git clone <repository-url>
cd instagram_bot
```

#### الطريقة الثانية: تحميل يدوي
1. حمل ملفات المشروع
2. استخرج الملفات
3. افتح Terminal/Command Prompt في مجلد المشروع

### الخطوة 2: إنشاء بيئة افتراضية (اختياري لكن موصى به)

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# macOS/Linux
python3 -m venv venv
source venv/bin/activate
```

### الخطوة 3: تثبيت المكتبات

```bash
pip install -r requirements.txt
```

أو تثبيت يدوي:
```bash
pip install python-telegram-bot==22.5
pip install aiohttp==3.13.3
pip install requests==2.31.0
```

### الخطوة 4: الحصول على توكن تلجرام

1. افتح تطبيق تلجرام
2. ابحث عن [@BotFather](https://t.me/botfather)
3. أرسل الأمر `/start`
4. أرسل الأمر `/newbot`
5. اتبع التعليمات:
   - أدخل اسم البوت (مثل: Instagram Username Checker)
   - أدخل اسم المستخدم للبوت (يجب أن ينتهي بـ bot، مثل: insta_checker_bot)
6. ستحصل على التوكن (مثال: `123456789:ABCdefGHIjklmnoPQRstuvWXYZ`)

### الخطوة 5: إعداد البوت

#### الطريقة الأولى: تحديث مباشر في الكود

افتح ملف `telegram_bot.py` وابحث عن:
```python
TOKEN = "YOUR_TELEGRAM_BOT_TOKEN"
```

استبدل `YOUR_TELEGRAM_BOT_TOKEN` بالتوكن الخاص بك:
```python
TOKEN = "123456789:ABCdefGHIjklmnoPQRstuvWXYZ"
```

#### الطريقة الثانية: استخدام متغيرات البيئة (آمن أكثر)

**Windows:**
```bash
set TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklmnoPQRstuvWXYZ
```

**macOS/Linux:**
```bash
export TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklmnoPQRstuvWXYZ
```

ثم عدّل `telegram_bot.py`:
```python
import os
TOKEN = os.getenv("TELEGRAM_BOT_TOKEN", "YOUR_TELEGRAM_BOT_TOKEN")
```

### الخطوة 6: تشغيل البوت

```bash
python3 telegram_bot.py
```

يجب أن ترى رسالة مثل:
```
🤖 البوت يعمل الآن...
```

## ✅ التحقق من التثبيت

### اختبار المكونات

```bash
python3 test_bot.py
```

يجب أن ترى جميع الاختبارات تمر بنجاح.

### اختبار البوت

1. افتح تطبيق تلجرام
2. ابحث عن البوت باسم المستخدم الذي أنشأته
3. أرسل `/start`
4. يجب أن ترى القائمة الرئيسية

## 🐛 استكشاف الأخطاء الشائعة

### خطأ: "No module named 'telegram'"

**الحل:**
```bash
pip install python-telegram-bot
```

### خطأ: "Invalid token"

**الحل:**
- تأكد من نسخ التوكن بشكل صحيح
- تأكد من عدم وجود مسافات إضافية
- جرب الحصول على توكن جديد من BotFather

### خطأ: "Connection refused"

**الحل:**
- تأكد من الاتصال بالإنترنت
- تأكد من أن التوكن صحيح
- جرب إعادة تشغيل البوت

### خطأ: "Database is locked"

**الحل:**
```bash
# حذف قاعدة البيانات
rm instagram_usernames.db

# أعد تشغيل البوت
python3 telegram_bot.py
```

### خطأ: "ModuleNotFoundError"

**الحل:**
```bash
# تأكد من تثبيت جميع المكتبات
pip install -r requirements.txt

# أو ثبت المكتبة الناقصة مباشرة
pip install <module-name>
```

## 🔄 تحديث البوت

```bash
# تحديث المكتبات
pip install -r requirements.txt --upgrade

# أو تحديث مكتبة معينة
pip install python-telegram-bot --upgrade
```

## 📁 هيكل المشروع بعد التثبيت

```
instagram_bot/
├── venv/                      # البيئة الافتراضية (إن وجدت)
├── username_generator.py      # مولد اليوزرات
├── instagram_checker.py       # مدقق إنستجرام
├── database.py               # إدارة قاعدة البيانات
├── telegram_bot.py           # البوت الرئيسي
├── config.py                 # الإعدادات
├── test_bot.py              # الاختبارات
├── requirements.txt         # المكتبات المطلوبة
├── instagram_usernames.db   # قاعدة البيانات (تُنشأ تلقائياً)
├── README.md               # دليل الاستخدام
└── INSTALLATION.md         # هذا الملف
```

## 🚀 التشغيل المستمر

### على Linux/macOS

استخدم `screen` أو `tmux`:

```bash
# باستخدام screen
screen -S instagram_bot
python3 telegram_bot.py

# للخروج: Ctrl+A ثم D
# للعودة: screen -r instagram_bot
```

### على Windows

استخدم Task Scheduler أو قم بإنشاء ملف batch:

```batch
@echo off
python3 telegram_bot.py
pause
```

## 📊 مراقبة البوت

### عرض السجلات

```bash
# عرض آخر 50 سطر
tail -n 50 bot.log

# مراقبة السجلات في الوقت الفعلي
tail -f bot.log
```

### إيقاف البوت

```bash
# على Linux/macOS
Ctrl+C

# أو قتل العملية
pkill -f telegram_bot.py
```

## 🔐 الأمان

### نصائح أمان مهمة

1. **لا تشارك التوكن**: التوكن هو كلمة مرور البوت
2. **استخدم متغيرات البيئة**: لا تضع التوكن في الكود مباشرة
3. **حماية قاعدة البيانات**: ضع المشروع في مكان آمن
4. **تحديثات منتظمة**: حدّث المكتبات بانتظام

## 📞 الدعم والمساعدة

إذا واجهت مشاكل:

1. تحقق من [README.md](README.md)
2. تحقق من رسائل الخطأ بعناية
3. جرب البحث عن الخطأ على Google
4. فتح issue على GitHub

## ✨ الخطوات التالية

بعد التثبيت الناجح:

1. اقرأ [README.md](README.md) لفهم الاستخدام
2. جرب الأوامر المختلفة
3. استكشف الإعدادات المتقدمة
4. قم بتخصيص البوت حسب احتياجاتك

---

**تم إنشاؤه بـ ❤️ لتسهيل عملية التثبيت والإعداد**
